#include <stdio.h>
int main()
{ int i=2,n,sum=0;
	printf("enter uper limit of even no.",i);
	scanf("%d",&n);
	for(i=2;i<=n;i=i+2)
	{
		sum=sum+i;
		}
		printf(" sum of even no.between 1 to %d is\n=%d",n,sum);
		return 0;
		}
